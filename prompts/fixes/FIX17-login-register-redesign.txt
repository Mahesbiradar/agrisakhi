Project: AgriSakhi
Task: Redesign Login and Register pages — professional, descriptive, full layout

Both pages use the auth layout: split screen on desktop, single on mobile.

Create src/components/AuthLayout.jsx:

<div className="min-h-screen w-full flex">
  
  {/* LEFT PANEL — branding, desktop only */}
  <div className="hidden lg:flex lg:w-1/2 flex-col justify-between p-12"
       style={{background: 'linear-gradient(135deg, #1a4731 0%, #2d6a4f 50%, #52b788 100%)'}}>
    
    {/* Logo */}
    <div className="flex items-center gap-3">
      <div className="w-10 h-10 bg-white/20 rounded-xl flex items-center 
                      justify-center text-2xl">🌿</div>
      <div>
        <p className="text-white font-bold text-lg">AgriSakhi</p>
        <p className="text-green-200 text-xs tracking-widest">KARNATAKA RURAL NETWORK</p>
      </div>
    </div>
    
    {/* Center illustration area */}
    <div className="flex-1 flex flex-col items-center justify-center gap-8">
      {/* Large emoji illustration */}
      <div className="text-center">
        <div className="text-8xl mb-4">🌾</div>
        <h2 className="text-white text-3xl font-bold leading-tight">
          "Connecting farmers<br/>with the right hands<br/>at the right time"
        </h2>
        <p className="text-green-200 mt-4 text-lg">
          ರೈತರ ಮತ್ತು ಕೂಲಿಕಾರರ ಸೇತು
        </p>
      </div>
      
      {/* Feature pills */}
      <div className="flex flex-col gap-3 w-full max-w-xs">
        {[
          { icon: '📍', text: 'Location-based matching' },
          { icon: '🎙️', text: 'Voice job descriptions' },
          { icon: '💬', text: 'Direct WhatsApp connect' },
          { icon: '🌐', text: 'English & ಕನ್ನಡ support' },
        ].map(f => (
          <div key={f.text} className="flex items-center gap-3 bg-white/10 
                                        rounded-xl px-4 py-3">
            <span className="text-xl">{f.icon}</span>
            <span className="text-white text-sm">{f.text}</span>
          </div>
        ))}
      </div>
    </div>
    
    {/* Bottom note */}
    <p className="text-green-300 text-xs">
      Free to use · Works on all phones · No subscription
    </p>
  </div>
  
  {/* RIGHT PANEL — form */}
  <div className="w-full lg:w-1/2 flex items-center justify-center 
                  bg-gray-50 p-6 min-h-screen">
    <div className="w-full max-w-md">
      {children}
    </div>
  </div>
  
</div>

--- src/pages/LoginPage.jsx --- REDESIGN ---

Wrap with AuthLayout.

Top of form:
  Mobile only: show small logo
    <div className="flex items-center gap-2 mb-6 lg:hidden">
      <span className="text-2xl">🌿</span>
      <span className="font-bold text-green-800">AgriSakhi</span>
    </div>
  
  Heading: "Welcome back 👋" / "ಮರಳಿ ಸ್ವಾಗತ 👋"
  Subtext: "Login to your AgriSakhi account"

Role indicator pills (visual only, no function):
  Small row: 🌾 Farmers  👷 Labourers  🚜 Providers
  "Login works for all roles" below in gray-400 text-xs

Form fields:
  Phone input (PhoneInput component with +91)
  Password input with show/hide eye toggle button

"Login" button:
  Full width, green-600, rounded-xl, py-4, text-base font-semibold
  Loading state: spinner + "Logging in..."

Below button:
  "Forgot Password?" link (right aligned)
  
Divider: ── or ──
  
"New to AgriSakhi?" centered
Three role register links in a row:
  "🌾 Farmer" | "👷 Labour" | "🚜 Provider"
  Each → /register?role=farmer/labour/provider
  Small pills, outlined green

--- src/pages/RegisterPage.jsx --- REDESIGN ---

Wrap with AuthLayout.

Step 1:
  Heading: "Create Your Account" 
  Subtext: "Join thousands of farmers across Karnataka"
  
  Show selected role prominently at top:
  <div className="flex items-center gap-3 bg-green-50 border border-green-200 
                  rounded-xl p-3 mb-6">
    <span className="text-3xl">{roleEmoji}</span>
    <div>
      <p className="font-semibold text-green-800">{roleName}</p>
      <button onClick={() => navigate(-1)} 
              className="text-xs text-green-600 underline">
        Change role
      </button>
    </div>
  </div>

  Fields with icons:
  👤 Full Name input
  📱 Phone (PhoneInput)
  🔒 Password (show/hide toggle)
  
  Progress: Step 1 of 2 (pill indicator, green fill)
  "Next →" green button

Step 2:
  Heading: "Almost done! 🎉"
  Subtext: "Allow location to find opportunities near you"
  
  Big centered location card:
  <div className="text-center bg-green-50 border-2 border-dashed 
                  border-green-300 rounded-2xl p-8 mb-6">
    <div className="text-6xl mb-4">📍</div>
    <h3 className="font-bold text-green-800 text-lg">Enable Location</h3>
    <p className="text-gray-600 text-sm mt-2">
      We use GPS to show you nearby {roleSpecificText}.
      Your location is never shared publicly.
    </p>
    <p className="text-green-600 text-sm mt-1">
      ಹತ್ತಿರದ ಅವಕಾಶಗಳಿಗಾಗಿ ಸ್ಥಳ ಅನುಮತಿ ಕೊಡಿ
    </p>
  </div>
  
  roleSpecificText:
    farmer → "labourers and services"
    labour → "job postings"  
    provider → "farmers looking for services"

  "Allow Location & Create Account 📍" green button (full width, py-4)
  
  After tapping: show inline loading "Getting your location... 📡"
  
  "Skip & Continue without location" small gray link below
  (registers without lat/lng, can enable later from dashboard)

npm run build — 0 errors.