Project: AgriSakhi
Task: Professional landing page — Claude design system, real animations, 
      image-driven, no emoji clutter, SaaS-quality

Install if not present: npm install framer-motion

src/pages/LandingPage.jsx — COMPLETE REWRITE

Remove ALL emojis from the landing page. Replace with SVG icons or images.
Use Unsplash free images via URL (no download needed).
Design inspiration: Linear.app, Vercel, Raycast — clean, dark hero, 
animated, content-rich but not cluttered.

--- COLOR TOKENS ---
const colors = {
  heroBg: '#0a1f0e',        // near-black green
  heroMid: '#0d2b12',
  accent: '#4ade80',        // bright green accent
  accentMuted: '#16a34a',
  text: '#f0fdf4',
  textMuted: '#86efac',
  textDim: '#4ade8066',
  card: '#ffffff0d',        // glass card
  cardBorder: '#ffffff1a',
}

--- SECTION 1: HERO (100vh, full bleed dark green) ---

Background: 
  <div style={{
    background: 'radial-gradient(ellipse 80% 60% at 50% -10%, #166534 0%, #0a1f0e 60%)',
    minHeight: '100vh', width: '100%', position: 'relative', overflow: 'hidden'
  }}>

Background texture layer (SVG noise overlay, very subtle):
  <svg style={{position:'absolute',inset:0,width:'100%',height:'100%',opacity:0.03}}>
    <filter id="noise">
      <feTurbulence type="fractalNoise" baseFrequency="0.65" numOctaves="3"/>
      <feColorMatrix type="saturate" values="0"/>
    </filter>
    <rect width="100%" height="100%" filter="url(#noise)"/>
  </svg>

Background grid lines (subtle):
  <div style={{
    position:'absolute', inset:0,
    backgroundImage: `linear-gradient(rgba(74,222,128,0.05) 1px, transparent 1px),
                      linear-gradient(90deg, rgba(74,222,128,0.05) 1px, transparent 1px)`,
    backgroundSize: '60px 60px'
  }}/>

NAV BAR (fixed, top-0, full width, z-50):
  Background: transparent initially, on scroll adds backdrop-blur + bg-black/40
  (use useEffect + window.scroll listener to toggle class)
  
  Left: 
    SVG leaf icon (simple, green, 24px) + "AgriSakhi" white bold + 
    small pill "Karnataka" in green-900/60 border-green-700 text-green-400 text-xs

  Right: 
    LanguageToggle (styled for dark bg — white text, transparent bg, border-white/20)
    "Login" button: border border-white/30 text-white px-4 py-2 rounded-lg 
                    hover:bg-white/10 transition

HERO CONTENT (max-w-7xl mx-auto px-6, pt-32 pb-20):
  Two column grid (lg:grid-cols-2, gap-16, items-center):

  LEFT COLUMN:
    Eyebrow badge (framer-motion fadeIn, delay 0):
      <motion.div initial={{opacity:0,y:10}} animate={{opacity:1,y:0}} transition={{duration:0.5}}>
        <span style={{
          background:'linear-gradient(135deg, #166534, #14532d)',
          border:'1px solid #16a34a40',
          color:'#4ade80', fontSize:'12px', letterSpacing:'0.1em',
          padding:'6px 14px', borderRadius:'100px', display:'inline-block'
        }}>
          KARNATAKA AGRICULTURE PLATFORM
        </span>
      </motion.div>

    H1 (framer-motion, delay 0.1):
      <motion.h1 initial={{opacity:0,y:20}} animate={{opacity:1,y:0}} transition={{duration:0.6,delay:0.1}}
        style={{fontSize:'clamp(36px,5vw,64px)', fontWeight:800, lineHeight:1.1,
                color:'#f0fdf4', marginTop:'20px'}}>
        Connecting<br/>
        Karnataka's<br/>
        <span style={{color:'#4ade80'}}>Farmers &</span><br/>
        <span style={{color:'#4ade80'}}>Workers</span>
      </motion.h1>

    Subtext (framer-motion, delay 0.2):
      style: fontSize 18px, color #86efac, maxWidth 440px, lineHeight 1.6, marginTop 20px
      Text: "Post farming jobs, find skilled labour nearby, and discover 
             agricultural services — all in one platform."
      Below: small text in #4ade80, fontFamily serif or italic:
             "ರೈತರ ಮತ್ತು ಕೂಲಿಕಾರರ ಸೇತು"

    CTA BUTTONS ROW (framer-motion, delay 0.3, flex gap-3 flexWrap):
      Button 1 "Get Started Free":
        style: background #4ade80, color #052e16, fontWeight 700, fontSize 15px,
               padding '14px 28px', borderRadius 10px, border none,
               cursor pointer, transition all 0.2s
        hover: background #86efac, transform translateY(-1px)
        onClick → navigate('/register')

      Button 2 "Login →":
        style: background transparent, color #f0fdf4, fontWeight 500,
               padding '14px 24px', borderRadius 10px,
               border '1px solid rgba(255,255,255,0.25)', cursor pointer
        hover: background rgba(255,255,255,0.08)
        onClick → navigate('/login')

    TRUST ROW (framer-motion, delay 0.4):
      Flex row, gap-6, marginTop 32px, alignItems center:
      
      "Trusted by farmers across 10+ districts" in #86efac fontSize 13px
      
      A row of 5 small avatar circles (32px each, overlapping -8px):
        Each: rounded-full, different green shades bg, white initial letter
        Colors: #166534, #15803d, #16a34a, #22c55e, #4ade80
        Letters: M, R, K, P, S (representing Karnataka names)
      
      After avatars: "500+ users" in #4ade80 fontSize 13px fontWeight 600

  RIGHT COLUMN (hidden on mobile, lg:block):
    A real photo from Unsplash:
    <div style={{borderRadius:20, overflow:'hidden', position:'relative',
                 boxShadow:'0 40px 80px rgba(0,0,0,0.5)'}}>
      <img 
        src="https://images.unsplash.com/photo-1500382017468-9049fed747ef?w=700&q=80&auto=format"
        alt="Karnataka farmland"
        style={{width:'100%', height:440, objectFit:'cover', display:'block'}}
      />
      
      Overlay gradient at bottom:
      <div style={{
        position:'absolute', bottom:0, left:0, right:0, height:200,
        background:'linear-gradient(transparent, #0a1f0e)'
      }}/>
      
      Floating card overlay (bottom-left of image):
        <div style={{
          position:'absolute', bottom:20, left:20, right:20,
          background:'rgba(0,0,0,0.7)', backdropFilter:'blur(12px)',
          borderRadius:14, padding:'16px 20px',
          border:'1px solid rgba(255,255,255,0.1)'
        }}>
          <div style={{display:'flex', alignItems:'center', gap:12}}>
            <div style={{width:40,height:40,background:'#166534',
                         borderRadius:10,display:'flex',alignItems:'center',
                         justifyContent:'center'}}>
              SVG location pin icon (white, 20px)
            </div>
            <div>
              <p style={{color:'white',fontSize:14,fontWeight:600,margin:0}}>
                Live location matching
              </p>
              <p style={{color:'#86efac',fontSize:12,margin:0}}>
                Shows workers within 50km of your farm
              </p>
            </div>
            <div style={{marginLeft:'auto',background:'#4ade80',
                         borderRadius:100,padding:'4px 10px',
                         fontSize:11,fontWeight:700,color:'#052e16'}}>
              LIVE
            </div>
          </div>
        </div>
    </div>

    Three stat chips below image (flex, gap-3, marginTop 16):
      Each chip: glass card style, padding '10px 16px', borderRadius 10:
        background:'rgba(255,255,255,0.06)', border:'1px solid rgba(255,255,255,0.1)'
        Number: white bold 20px
        Label: #86efac 11px
      
      "500+" Farmers | "1200+" Workers | "50+" Services

SCROLL INDICATOR:
  Centered bottom of hero, animated bounce:
  <motion.div animate={{y:[0,8,0]}} transition={{duration:1.5,repeat:Infinity}}>
    SVG chevron down icon (white/40, 24px)
  </motion.div>

--- SECTION 2: HOW IT WORKS (bg white, py-24) ---

max-w-6xl mx-auto px-6

TOP: 
  Small label: "HOW IT WORKS" in green-600 text-xs tracking-widest font-semibold
  H2: "Three steps to connect" in gray-900 font-bold text-4xl mt-2
  Subtext: "Simple enough for first-time smartphone users" in gray-500

Timeline layout (NOT cards — use a connected timeline):
  Vertical line connecting 3 steps (hidden mobile, shown desktop):
  
  Each step: horizontal row (number | content | image):
    Step number: Large "01" "02" "03" in green-100 text-green-600 
                 font-black text-6xl opacity-30 (decorative)
    
    Content block:
      Icon: SVG icon in green circle (48px) — NO emojis
        Step 1: User/person SVG icon
        Step 2: Location pin SVG icon  
        Step 3: Phone/call SVG icon
      
      Title: 22px semibold gray-900
      Desc: 15px gray-500 leading-relaxed
      Kannada: 13px text-green-600 italic

    Right: Mockup screenshot placeholder:
      A phone-shaped div (240px × 420px, bg-gray-900, rounded-3xl, 
      border-4 border-gray-700, overflow-hidden):
        Inside: green gradient bg + simple text showing what the 
        step's screen looks like (just styled divs, no real screenshot)
        
        Step 1 phone mockup: shows registration form UI in mini
        Step 2 phone mockup: shows job list with location pins
        Step 3 phone mockup: shows call/WhatsApp buttons

  Steps alternate left/right on desktop (step 1: content left, mockup right;
  step 2: mockup left, content right; step 3: content left, mockup right)

framer-motion whileInView animations on each step.

--- SECTION 3: WHO IS IT FOR (bg-gray-50, py-24) ---

max-w-6xl mx-auto

H2: "Built for everyone in agriculture" centered gray-900
Subtext: "Whether you grow crops, work fields, or provide services" gray-500

3 cards (grid-cols-3 desktop, stacked mobile):
Each card: bg-white, rounded-2xl, overflow-hidden, shadow-sm,
           hover:shadow-xl hover:-translate-y-2 transition-all duration-300,
           border border-gray-100

  TOP IMAGE BAND (full width, 200px height):
    Farmer card: 
      <img src="https://images.unsplash.com/photo-1589923188651-268a9765e432?w=600&q=80&auto=format"
           style={{width:'100%',height:200,objectFit:'cover'}}/>
    Labour card:
      <img src="https://images.unsplash.com/photo-1599059813005-11265ba4b4ce?w=600&q=80&auto=format"
           style={{width:'100%',height:200,objectFit:'cover'}}/>
    Provider card:
      <img src="https://images.unsplash.com/photo-1592878904946-b3cd8ae243d4?w=600&q=80&auto=format"
           style={{width:'100%',height:200,objectFit:'cover'}}/>

  CARD BODY (padding 24px):
    Role label: colored pill (green/amber/blue) + role name
    Kannada name: gray-500 text-sm mt-1
    Feature list (4 items, custom SVG checkmark in role color, no emojis):
      text-sm text-gray-600 py-1 flex items-center gap-2
    CTA button: full width, role color, rounded-xl, mt-4
      "Join as Farmer" / "Find Work" / "List Services"

--- SECTION 4: STATS (bg-gray-900, py-16) ---

4 stats centered, flex, gap-16, wrap:
Each:
  Number: text-5xl font-black text-white
  Label: text-sm text-gray-400 mt-1
  
Dividers: 1px w-px h-12 bg-gray-700 (hidden mobile)

Add subtle animated counter effect using framer-motion useInView + 
count up animation when section scrolls into view:

const Counter = ({target, suffix='+'}) => {
  const [count, setCount] = useState(0)
  const ref = useRef(null)
  const inView = useInView(ref, {once: true})
  
  useEffect(() => {
    if (!inView) return
    let start = 0
    const duration = 1500
    const step = target / (duration / 16)
    const timer = setInterval(() => {
      start += step
      if (start >= target) { setCount(target); clearInterval(timer) }
      else setCount(Math.floor(start))
    }, 16)
    return () => clearInterval(timer)
  }, [inView, target])
  
  return <span ref={ref}>{count}{suffix}</span>
}

Stats: <Counter target={2000}/> Users | <Counter target={500}/> Jobs | 
       <Counter target={50}/> Services | <Counter target={10}/> Districts

--- SECTION 5: CTA (bg gradient #0a1f0e → #052e16, py-20) ---

Centered:
  H2: "Start connecting today" white font-bold text-4xl
  p: "ಈಗಲೇ ಪ್ರಾರಂಭಿಸಿ" text-green-400 mt-2
  
  Two buttons (mt-8):
    "Create Free Account" → bg-green-400 text-black font-bold 
                            px-8 py-4 rounded-xl text-lg
                            hover:bg-green-300
    "Login →" → border border-white/30 text-white px-8 py-4 
                rounded-xl hover:bg-white/10

  Below: "Free to use · No subscription · Works on all phones"
    text-green-700 text-sm mt-6

FOOTER (bg-black, py-5):
  flex justify-between px-8:
  Left: "© 2026 AgriSakhi · Karnataka" text-gray-500 text-sm
  Right: "Kisan Helpline: 1800-180-1551" text-gray-500 text-sm

--- SVG ICONS TO USE (inline, no emoji) ---
For all icons throughout the page use simple inline SVG paths:

Location pin:
<svg viewBox="0 0 24 24" fill="currentColor">
  <path d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7zm0 9.5c-1.38 0-2.5-1.12-2.5-2.5s1.12-2.5 2.5-2.5 2.5 1.12 2.5 2.5-1.12 2.5-2.5 2.5z"/>
</svg>

Phone:
<svg viewBox="0 0 24 24" fill="currentColor">
  <path d="M6.62 10.79c1.44 2.83 3.76 5.14 6.59 6.59l2.2-2.2c.27-.27.67-.36 1.02-.24 1.12.37 2.33.57 3.57.57.55 0 1 .45 1 1V20c0 .55-.45 1-1 1-9.39 0-17-7.61-17-17 0-.55.45-1 1-1h3.5c.55 0 1 .45 1 1 0 1.25.2 2.45.57 3.57.11.35.03.74-.25 1.02l-2.2 2.2z"/>
</svg>

User/person:
<svg viewBox="0 0 24 24" fill="currentColor">
  <path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/>
</svg>

Checkmark:
<svg viewBox="0 0 24 24" fill="currentColor">
  <path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z"/>
</svg>

Leaf (logo):
<svg viewBox="0 0 24 24" fill="currentColor">
  <path d="M17 8C8 10 5.9 16.17 3.82 21.34L5.71 22l1-2.3A4.49 4.49 0 0 0 8 20C19 20 22 3 22 3c-1 2-8 2-8 2s2-6-7-3z"/>
</svg>

--- PERFORMANCE NOTES ---
- All Unsplash images use ?w=600&q=80 params — auto-compressed
- framer-motion: only animate opacity, y, scale — no layout animations
- useInView with once:true — animate only first time in viewport
- No heavy libraries beyond framer-motion

npm run build — 0 errors.
Verify all 4 sections visible on desktop.
Verify mobile layout stacks correctly (no overflow).